# Copy to .env locally (never commit real credentials)
PORT=10000

# Aiven MySQL (set these on Render as Environment Variables)
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=

# If Aiven requires SSL, set DB_SSL=true
DB_SSL=false

